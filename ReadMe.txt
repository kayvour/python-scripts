A repository containing a collection of useful Python scripts for various tasks.

Prerequisites
Python 3.7 or later installed.

Usage
Each script in the repository comes with its own usage instructions and can be run independently. See the script headers or inline comments for details.

Requirements
The following Python packages are used across the scripts:
nltk>=3.7
textstat>=0.7.1
textblob>=0.17.1
pypdf2>=3.0.1
pytubefix>=9.1.1